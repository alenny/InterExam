extends layout

block content
    p Choose an exam to take:
    ul
        each lib in libs
            li
                a(href='/exam/#{lib.name}')= lib.displayName
    p Other resources:
    ul
        li  
            a(href='/users')
                | All users

